"""2D point helpers (scenario 1: signature change)."""


def scale_point(x, y, factor):
    """Scale a point's coordinates by ``factor`` and return the new (x, y)."""
    return (x * factor, y * factor)
